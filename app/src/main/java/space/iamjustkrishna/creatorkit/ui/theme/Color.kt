package space.iamjustkrishna.creatorkit.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// --- PRIMARY BRAND COLORS (Electric Violet) ---
val PrimaryBlue = Color(0xFF6C63FF)      // Main buttons, active states (Cool Violet)
val PrimaryDark = Color(0xFF5046E5)      // Darker shade for contrast
val OnPrimary = Color(0xFFFFFFFF)        // Text on primary buttons

// --- SECONDARY COLORS (Teal/Cyan accents) ---
val SecondaryTeal = Color(0xFF00BFA5)
val OnSecondary = Color(0xFFFFFFFF)

// --- BACKGROUNDS (The "Midnight Studio" Look) ---
// Dark Mode Backgrounds
val DarkBackground = Color(0xFF121212)   // Almost black
val DarkSurface = Color(0xFF1E1E24)      // Cards/Containers (Slightly lighter)
val DarkSurfaceVariant = Color(0xFF2B2B36) // Input fields/Secondary cards

// Light Mode Backgrounds (Clean & Minimal)
val LightBackground = Color(0xFFF4F6F8)  // Soft grey-white
val LightSurface = Color(0xFFFFFFFF)     // Pure white cards
val LightSurfaceVariant = Color(0xFFE1E2E6)

// --- TEXT COLORS ---
val TextWhite = Color(0xFFEEEEEE)
val TextGray = Color(0xFFAAAAAA)
val TextBlack = Color(0xFF1A1C1E)